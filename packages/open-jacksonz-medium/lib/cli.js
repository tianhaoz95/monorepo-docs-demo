#!/usr/bin/env node

const openJacksonzMedium = require('./open-jacksonz-medium')

/**
 * The entry point to the CLI
 * In this example, it calls the
 * function that opens JacksonZ's
 * Medium profile in the default
 * browser.
 */
openJacksonzMedium()
